import 'package:flutter/material.dart';
class RegistroCortesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Registro de Cortes")),
      body: Center(child: Text("Aquí tu página de Registro de Cortes")),
    );
  }
}